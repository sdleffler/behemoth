[package]
name = "behemoth"
version = "0.3.1"
authors = ["Sean Leffler <sean@errno.com>"]

[features]
default = ["as_mathematica", "matrix_rand", "vector_rand"]
as_mathematica = []
matrix_rand = ["rand"]
vector_rand = ["rand"]

[dependencies]
rand = { version = "0.3", optional = true }
